[package]
name    = "tokimo-bus-cli"
version = "0.1.0"
edition = "2024"
rust-version.workspace = true

[dependencies]
clap   = { version = "4", features = ["derive", "env"] }
anyhow = "1"
serde  = { version = "1", features = ["derive"] }
toml   = "0.8"

[lints]
workspace = true
